#include<stdio.h>

int main(){
    float principal=100, rate=4, years=1;
    float simpleinterest = (principal*rate*years*0.01);
    printf("The value of simple interst is %f",simpleinterest);
    
    return 0;
}
